# Calculator
A simple calculator written in C.

### Usage:
```
Help:
 -v = version
 -id = input decimal
 -ih = input hexadecimal
 -ib = input binary
 -od = output decimal
 -oh = output hexadecimal
 -ob = output binary
```
